[phases.setup]
nixPkgs = ["python311", "nodejs-18_x"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "npm install",
  "npm run build"
]

[phases.build]
cmds = ["python migrate_database_schema.py || true"]

[start]
cmd = "uvicorn server_py.main:app --host 0.0.0.0 --port $PORT"
